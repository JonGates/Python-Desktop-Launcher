<UserControl x:Class="ProjectLauncher.Desktop.Controls.ToastHost"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:loc="clr-namespace:ProjectLauncher.Desktop.Services" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Visibility="{Binding HasNotification, Converter={StaticResource BoolVisibility}}"
 Focusable="False">
 <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource AccentBrush}"
         BorderThickness="1" CornerRadius="10" Padding="12" MaxHeight="240">
  <Grid>
   <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
   <TextBlock Text="!" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="0,0,10,0"/>
   <StackPanel Grid.Column="1">
    <TextBlock Text="{Binding Notification}" TextWrapping="Wrap" MaxHeight="160" TextTrimming="CharacterEllipsis" FontSize="12"/>
    <Button Content="{loc:LocalizedText Ui.018}" Style="{StaticResource TextButton}" HorizontalAlignment="Left" Padding="0,6,8,0" Click="Details_Click"/>
   </StackPanel>
   <Button Grid.Column="2" Content="×" ToolTip="{loc:LocalizedText Ui.019}" AutomationProperties.Name="{loc:LocalizedText Ui.019}"
           Style="{StaticResource TextButton}" VerticalAlignment="Top" Padding="8,0" Click="Dismiss_Click"/>
  </Grid>
 </Border>
</UserControl>
