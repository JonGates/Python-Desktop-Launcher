<UserControl x:Class="ProjectLauncher.Desktop.Views.RunView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:loc="clr-namespace:ProjectLauncher.Desktop.Services" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:controls="clr-namespace:ProjectLauncher.Desktop.Controls">
  <UserControl.Resources>
    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}"><Setter Property="MinHeight" Value="34"/><Setter Property="Padding" Value="10,7"/></Style>
    <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}"><Setter Property="MinHeight" Value="34"/></Style>
  </UserControl.Resources>
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition x:Name="ParameterRow" Height="*"/><RowDefinition Height="0.75*"/></Grid.RowDefinitions>
    <Grid Margin="0,0,0,14">
      <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel>
        <TextBlock x:Name="ActionTitle" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
        <TextBlock x:Name="ActionSubtitle" Style="{StaticResource Hint}" Margin="0,4,0,0" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
      </StackPanel>
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
        <Button Content="{loc:LocalizedText Ui.048}" Click="OpenOutput_Click" Style="{StaticResource TextButton}"/>
        <Button Content="{loc:LocalizedText Ui.049}" Click="Settings_Click" Padding="12,7" MinHeight="34" Margin="6,0,0,0"/>
      </StackPanel>
    </Grid>
    <StackPanel x:Name="EmptyPanel" Grid.Row="1" Margin="0,12,0,24"><TextBlock Text="{loc:LocalizedText Ui.050}" Style="{StaticResource SectionTitle}"/><Button Content="{loc:LocalizedText Ui.051}" Click="Settings_Click" HorizontalAlignment="Left" Margin="0,12,0,0"/></StackPanel>
    <Border x:Name="ExecutionBar" Grid.Row="1" Style="{StaticResource Card}" Padding="14,10" CornerRadius="10" Margin="0,0,0,12">
      <Grid>
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
          <TextBlock Text="{Binding RuntimeLabel}" Foreground="{DynamicResource AccentBrush}" FontSize="11" FontWeight="SemiBold"/>
          <TextBlock Text="{Binding Status}" Style="{StaticResource Hint}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Margin="0,3,0,0"/>
        </StackPanel>
        <TextBlock Grid.Column="1" Text="{Binding Elapsed}" FontFamily="{StaticResource MonoFont}" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" Margin="0,0,16,0"/>
        <Button Grid.Column="2" x:Name="StopButton" Content="{loc:LocalizedText Ui.052}" Click="Stop_Click" IsEnabled="{Binding Busy}" MinHeight="36" Padding="18,7" Margin="0,0,8,0"/>
        <Button Grid.Column="3" x:Name="StartButton" Content="{loc:LocalizedText Ui.053}" Style="{StaticResource PrimaryButton}" Click="Start_Click" IsEnabled="{Binding CanRun}" MinHeight="36" Padding="20,7"/>
      </Grid>
    </Border>
    <Border x:Name="ParameterCard" Grid.Row="2" Style="{StaticResource Card}" Padding="16,12" CornerRadius="10" Margin="0,0,0,12">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,12">
          <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
          <TextBlock Text="{loc:LocalizedText Ui.054}" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,18,0"/>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <ComboBox x:Name="PresetBox" Width="160" SelectionChanged="Preset_Changed" ToolTip="{loc:LocalizedText Ui.055}"/>
            <Button Content="＋" ToolTip="{loc:LocalizedText Ui.056}" Style="{StaticResource IconButton}" Click="SavePreset_Click" Margin="6,0,0,0"/>
            <Button Content="−" ToolTip="{loc:LocalizedText Ui.057}" Style="{StaticResource IconButton}" Click="DeletePreset_Click" Margin="4,0,0,0"/>
          </StackPanel>
          <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="12,0,0,0">
            <CheckBox x:Name="AdvancedCheck" Content="{loc:LocalizedText Ui.058}" FontSize="11" Checked="Advanced_Changed" Unchecked="Advanced_Changed" VerticalAlignment="Center"/>
            <Button Content="{loc:LocalizedText Ui.059}" Style="{StaticResource TextButton}" Click="Reset_Click" Margin="8,0,0,0"/>
          </StackPanel>
        </Grid>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <controls:ParameterForm x:Name="Form" Margin="0,0,8,0" IsEnabled="{Binding CanRun}"/>
        </ScrollViewer>
        <Expander Grid.Row="2" Header="{loc:LocalizedText Ui.060}" Foreground="{DynamicResource SecondaryText}" FontSize="11" Margin="0,8,0,0">
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <Grid.RowDefinitions><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
            <TextBox x:Name="PreviewBox" IsReadOnly="True" TextWrapping="Wrap" FontFamily="{StaticResource MonoFont}" FontSize="11" MaxHeight="70" VerticalScrollBarVisibility="Auto"/>
            <Button Grid.Column="1" Content="{loc:LocalizedText Ui.061}" Style="{StaticResource TextButton}" Click="CopyCommand_Click" Margin="6,0,0,0" VerticalAlignment="Top"/>
            <TextBlock Grid.Row="1" x:Name="PreviewHint" Style="{StaticResource Hint}" FontSize="10" Margin="0,4,0,0" MaxHeight="38"/>
          </Grid>
        </Expander>
      </Grid>
    </Border>
    <Border Grid.Row="3" Style="{StaticResource Card}" Padding="14,10" CornerRadius="10">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <DockPanel>
          <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
            <CheckBox x:Name="AutoScrollCheck" Content="{loc:LocalizedText Ui.062}" IsChecked="True" FontSize="11" VerticalAlignment="Center"/>
            <Button Content="{loc:LocalizedText Ui.063}" Style="{StaticResource TextButton}" Click="ExportLog_Click" Margin="8,0,0,0" ToolTip="{loc:LocalizedText Ui.064}"/>
            <Button Content="{loc:LocalizedText Ui.065}" Style="{StaticResource TextButton}" Click="ClearLog_Click"/>
          </StackPanel>
          <TextBlock Text="{loc:LocalizedText Ui.066}" FontWeight="SemiBold" VerticalAlignment="Center"/>
        </DockPanel>
        <ProgressBar Grid.Row="1" Value="{Binding Progress, Mode=OneWay}" IsIndeterminate="{Binding IsIndeterminate}" Height="3" Margin="0,4,0,6" Visibility="{Binding Busy, Converter={StaticResource BoolVisibility}}"/>
        <TabControl Grid.Row="2" Margin="0,4,0,0">
          <TabItem Header="{loc:LocalizedText Ui.067}"><TextBox x:Name="LogBox" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True" FontFamily="{StaticResource MonoFont}" FontSize="12" Background="{DynamicResource TerminalBackground}" Foreground="{DynamicResource TerminalForeground}" BorderThickness="0" AcceptsReturn="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/></TabItem>
          <TabItem Header="{loc:LocalizedText Ui.068}"><ListBox x:Name="HistoryList" ItemsSource="{Binding History}" MouseDoubleClick="History_DoubleClick"><ListBox.ItemTemplate><DataTemplate><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="110"/><ColumnDefinition Width="*"/><ColumnDefinition Width="80"/><ColumnDefinition Width="75"/></Grid.ColumnDefinitions><TextBlock Text="{Binding Started, StringFormat={}{0:MM-dd HH:mm}}" Style="{StaticResource Hint}"/><TextBlock Grid.Column="1" Text="{Binding ActionLabel}"/><TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="{DynamicResource AccentBrush}"/><TextBlock Grid.Column="3" Text="{Binding DurationSeconds, StringFormat={}{0:0.0}s}" Style="{StaticResource Hint}"/></Grid></DataTemplate></ListBox.ItemTemplate></ListBox></TabItem>
        </TabControl>
      </Grid>
    </Border>
  </Grid>
</UserControl>
